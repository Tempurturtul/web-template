{
  "private": true,
  "devDependencies": {
    "babel-core": "^6.25.0",
    "babel-loader": "^7.0.0",
    "babel-preset-env": "^1.5.2",
    "css-loader": "^0.28.4",
    "extract-text-webpack-plugin": "^2.1.2",
    "webpack": "^2.6.1",
    "webpack-dev-server": "^2.4.5"
  },
  "scripts": {
    "prebuild": "test ! -d dist || rm -r dist; mkdir dist",
    "build": "cp -r public/* dist && webpack",
    "serve": "webpack-dev-server"
  }
}
